varnishtest "test vmod-pat"

server s1 {
	rxreq
	txresp
} -start

varnish v1 -vcl+backend {
        import pat;

        sub vcl_deliver {
	    set resp.http.Hello = pat.hello();
        }
} -start

client c1 {
	txreq
	rxresp
	expect resp.status == 200
	expect resp.http.Hello == "vmod-pat"
} -run
